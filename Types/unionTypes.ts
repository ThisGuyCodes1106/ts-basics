//LITERAL TYPES

//variable zero can only be the number 0
let zero: 0 = 0

//initialized as "Happy", can only be happy or sad strings
let mood: "Happy" | "Sad" = "Happy"

type DayOfWeek = "Push" | "Pull" | "Legs"
let today: DayOfWeek = "Push"